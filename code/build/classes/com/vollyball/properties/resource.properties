# To change this license header, choose License Headers in Project Properties.
# To change this template file, choose Tools | Templates
# and open the template in the editor.

db.name=vollyball.db

create.competition = CREATE TABLE Competition (id INTEGER PRIMARY KEY AUTOINCREMENT, name VARCHAR(255) NOT NULL, venue VARCHAR(255) NOT NULL,start_date DATE NOT NULL,end_date DATE NOT NULL,phase DATE NOT NULL);

insert.signUp=insert into user_detail(id,username,emailid,password) values(?,?,?,?)

